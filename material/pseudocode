stack current_path;  //Save the current path

MSA(start, end)

Push the start position into the stack current_path
Get the top element n of the stack current_path

if n equal to the position of end
    save the stack current_path to all_path
    end
else
    if left hand direction of robot is able to explore
        i = the position of next point in left hand direction direction
    if positive direction of robot is able to explore
        i = the position of next point in positive direction
    if right hand position of robot is able to explore
        i = the position of next point in right hand position
    if opposite direction of robot is able to explore
        i = the position of next point in  hand opposite position
    MSA(i, end)



